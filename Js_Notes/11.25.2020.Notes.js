function match(candidate, job) {
    /// figure out calculation between min and mix
    //check if the candidate has min /max
    let  candidate1 =  true
    let  candidate1 = false
    // candidiate  job mix / max
    /// job  salary min / max 
    // less than or equavalet to job max. plus 10%
    } return 
    // is this job a valid match for the candidate?
  }